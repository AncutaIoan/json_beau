[package]
name = "json_beau"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.31.1"  # Provides the core `egui` app support
serde_json = "1.0"  # To parse JSON
serde = { version = "1.0", features = ["derive"] }  # Serialization/deserialization
